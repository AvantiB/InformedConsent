pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3
tabulate>=0.9
tqdm>=4.66

# Optional structured classifier. If unavailable, the runner skips structured_xgb.
xgboost>=2.0

# Optional semantic features. Install/use these only when running
# --embedding_model or --nli_model. The script can compute embeddings via
# plain Hugging Face transformers mean pooling, so sentence-transformers is
# useful but not required.
torch>=2.1
transformers>=4.40
sentence-transformers>=2.6
